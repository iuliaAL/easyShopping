spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/EasyShopping?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: iulAdo98
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        id:
          new_generator_mappings: "false"
        format_sql: "true"
        globally_quoted_identifiers: "true"
    show-sql: "true"
    hibernate:
      ddl-auto: update
  messages:
    basename: validation
logging:
  level:
    org:
      hibernate:
        SQL: debug
jdbcCompliantTruncation: "false"
management:
  security:
    enabled: "false"

